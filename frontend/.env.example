# URL del backend, usato SOLO in sviluppo locale (frontend e backend
# girano su porte diverse). NON impostare questa variabile su Vercel:
# in produzione, con Vercel Services, il frontend chiama automaticamente
# un percorso relativo (/api/...) sullo stesso dominio.
VITE_BACKEND_URL=http://localhost:3001
